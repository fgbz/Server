<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="phalaenopsis.lawcase.dao.ICoordinateDao">

	<select id="getLawCaseShape" resultType="String">
		select SDO_UTIL.to_gmlgeometry(
		<if test="realShape == true">
			RealShape
		</if>
		<if test="realShape == false">
			Shape
		</if>
		) from LAW_CASEBASEINFO where id=#{id}
	</select>

	<update id="updateLawCaseShape">
		update LAW_CASEBASEINFO set
		<if test="realShape == true">
			RealShape
		</if>
		<if test="realShape == false">
			Shape
		</if>
		= ${strCoordinate} where id=#{id}
	</update>


	<update id="deleteLawCaseShape">
		update LAW_CASEBASEINFO set
		<if test="realShape == true">
			RealShape
		</if>
		<if test="realShape == false">
			Shape
		</if>
		= null where id=#{id}
	</update>

	<insert id="testupdate" parameterType="SGeometry">
		update LAW_CASEBASEINFO set
		<if test="realShape == true">
			RealShape
		</if>
		<if test="realShape == false">
			Shape
		</if>
		= #{geometry, javaType=oracle.spatial.geometry.JGeometry,
		jdbcType=STRUCT}
		where id='61579546591236'
	</insert>

</mapper>